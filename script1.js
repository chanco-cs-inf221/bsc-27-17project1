var form= getElementById('form');
var store= getElementById('search');

function store(){
const form = document.querrySelector('form');
const button = document.querrySelector('search');

form.addEventlistener('search', function(event){
	event.preventDefault();
	var search = form.search.value;

	let arr = [search]

 localstorage.setItem("li1",arr):

var display = document.getElementById('display')
var links = document.createElement('u1');

links.setAttribute("href", "index.html");
links.setAttribute("class", "link"):

var form = document.createTextNode(search);
links.appendChild(search);
display.appendChild(links);
}
form.reset();